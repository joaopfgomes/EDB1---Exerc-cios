#ifndef AREA_H
#define AREA_H

#include "aresta.h"

// Função para calcular a área de uma face do cubo
float calcular_area_face(Cubo* c);

// Função para calcular a área total das faces do cubo
float calcular_area_total(Cubo* c);

#endif // AREA_H